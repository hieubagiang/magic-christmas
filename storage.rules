rules_version = '2';
service "firebase.storage" {
  match /b/{bucket}/o {
    match /uploads/{allPaths=**} {
      allow read: if true;
      allow write: if true; // Anonymous allowed for demo
    }
  }
}
